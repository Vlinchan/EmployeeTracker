package com.example.employeetracker.ui.theme

import androidx.compose.ui.graphics.Color

// --- VIBRANT BRAND COLORS ---
val ElectricViolet = Color(0xFF6200EA)   // Deep, rich purple (Primary)
val VioletLight = Color(0xFF9D46FF)      // Lighter purple for Dark Mode
val NeonTeal = Color(0xFF00BFA5)         // Punchy Teal (Secondary - Floating Buttons)
val HotPink = Color(0xFFFF4081)          // Energetic Pink (Tertiary - Highlights)

// --- BACKGROUNDS & SURFACES (The Fix for "White-on-White") ---
// Light Mode
val BackgroundIce = Color(0xFFF0F4F8)    // Cool light grey-blue (Not plain white!)
val SurfaceWhite = Color(0xFFFFFFFF)     // Pure white for Cards
val SearchBarLight = Color(0xFFE8EAF6)   // Distinct Soft Indigo for Search Bars

// Dark Mode
val BackgroundSpace = Color(0xFF12121F)  // Deep Navy/Black (Rich dark mode)
val SurfaceDark = Color(0xFF1E1E2E)      // Slightly lighter Navy for Cards
val SearchBarDark = Color(0xFF2B2B40)    // Visible distinct Grey-Blue

// --- STATUS COLORS (Bright & Clear) ---
val VividGreen = Color(0xFF00C853)       // Success
val VividOrange = Color(0xFFFFAB00)      // Warning
val VividRed = Color(0xFFD50000)         // Error

// --- Text Colors ---
val TextBlack = Color(0xFF1C1B1F)
val TextWhite = Color(0xFFE6E1E5)
val TextGrey = Color(0xFF787579)
